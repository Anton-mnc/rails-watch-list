<%= simple_form_for @list do |f| %>
  <%= f.input :name %>
  <%= f.input :photo, as: :file %>
  <%= f.submit "Submit list", class: "btn btn-primary" %>
<% end %>
<%= link_to "back to list", lists_path, class: "btn btn-danger"%>
